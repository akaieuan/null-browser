//! OpenAI-compatible cloud provider. Opt-in, user-supplied API key.
//!
//! Endpoint is configurable so the same implementation serves
//! OpenAI itself and any OpenAI-compatible server the user points
//! it at (local or otherwise).
